# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kvm_platform
DB_USER=postgres
DB_PASS=postgres

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=supersecretjwt
JWT_EXPIRES_IN=24h

# Email (nodemailer)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your@example.com
EMAIL_PASS=yourpassword
EMAIL_FROM="KVM Platform <no-reply@example.com>"

# Terraform base paths (adapt as needed)
TERRAFORM_BASE_DIR=../terraform
TERRAFORM_TEMPLATE_DIR=../terraform/cloud_img

# Default admin seed (optional)
DEFAULT_ADMIN_EMAIL=admin@gmail.com
DEFAULT_ADMIN_PASS=admin